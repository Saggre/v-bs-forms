import { VueWrapper, mount } from '@vue/test-utils';

describe('Calendar', () => {
  describe(':props', () => {
    it(':initial-page test', async () => {
      expect(true).toBe(true);
    });
  });
});
